# Copie este arquivo para .env.local (nunca commitar valores reais)

# URL pública da API (Fastify) — usada tanto no servidor (fetch inicial da
# campanha) quanto no navegador (envio do formulário de doação).
NEXT_PUBLIC_API_URL=http://localhost:3333
